import { PageHeader, SectionTitle, StatCard, EmptyState, StatusBadge } from "@/components/enterprise";

export { PageHeader, SectionTitle, StatusBadge };
export const KpiCard = StatCard;
export const Empty = EmptyState;
