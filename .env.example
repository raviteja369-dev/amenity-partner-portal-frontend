# Production / deployed builds (also set in your host's env vars)
REACT_APP_BACKEND_URL=https://amenity-partner-portal-backend-production.up.railway.app

# Vercel only — required for live landing-page stats (/api/stats/landing)
MONGO_URI=your_mongodb_atlas_connection_string
# DB_NAME=school-connect

# Local `npm start` uses /api proxy → http://localhost:5000 (see src/setupProxy.js)
